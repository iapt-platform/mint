# 由 openapi/scripts/generate.php 生成，请勿手改；补充写到 overrides/v2-nissaya-ending-export.yaml
get:
  summary: 'export nissaya-ending-export'
  tags:
    - nissaya-ending-export
  operationId: get_api_v2_nissaya_ending_export
  description: 实现：`NissayaEndingController@export`
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
