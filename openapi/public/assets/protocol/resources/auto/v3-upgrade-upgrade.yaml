# 由 openapi/scripts/generate.php 生成，请勿手改；补充写到 overrides/v3-upgrade-upgrade.yaml
get:
  summary: '获取单个 upgrade/{upgrade}'
  tags:
    - upgrade
  operationId: get_api_v3_upgrade_upgrade_
  description: 实现：`UpgradeController@show`
  parameters:
    -
      name: upgrade
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
put:
  summary: '更新 upgrade/{upgrade}'
  tags:
    - upgrade
  operationId: put_api_v3_upgrade_upgrade_
  description: 实现：`UpgradeController@update`
  parameters:
    -
      name: upgrade
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
patch:
  summary: '更新 upgrade/{upgrade}'
  tags:
    - upgrade
  operationId: patch_api_v3_upgrade_upgrade_
  description: 实现：`UpgradeController@update`
  parameters:
    -
      name: upgrade
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
delete:
  summary: '删除 upgrade/{upgrade}'
  tags:
    - upgrade
  operationId: delete_api_v3_upgrade_upgrade_
  description: 实现：`UpgradeController@destroy`
  parameters:
    -
      name: upgrade
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
