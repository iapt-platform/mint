# 由 openapi/scripts/generate.php 生成，请勿手改；补充写到 overrides/v2-discussion-count-discussion-count.yaml
get:
  summary: '获取单个 discussion-count/{discussion_count}'
  tags:
    - discussion-count
  operationId: get_api_v2_discussion_count_discussion_count_
  description: 实现：`DiscussionCountController@show`
  parameters:
    -
      name: discussion_count
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
                properties:
                  id:
                    type: integer
                  res_id:
                    type: integer
                  res_type:
                    type: string
                  type:
                    type: integer
                  editor_uid:
                    type: string
                  wbw:
                    type: string
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
put:
  summary: '更新 discussion-count/{discussion_count}'
  tags:
    - discussion-count
  operationId: put_api_v2_discussion_count_discussion_count_
  description: 实现：`DiscussionCountController@update`
  parameters:
    -
      name: discussion_count
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
patch:
  summary: '更新 discussion-count/{discussion_count}'
  tags:
    - discussion-count
  operationId: patch_api_v2_discussion_count_discussion_count_
  description: 实现：`DiscussionCountController@update`
  parameters:
    -
      name: discussion_count
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
delete:
  summary: '删除 discussion-count/{discussion_count}'
  tags:
    - discussion-count
  operationId: delete_api_v2_discussion_count_discussion_count_
  description: 实现：`DiscussionCountController@destroy`
  parameters:
    -
      name: discussion_count
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
