# 由 openapi/scripts/generate.php 生成，请勿手改；补充写到 overrides/v2-mock-openai-chat-completions.yaml
post:
  summary: '模拟 Chat Completions API'
  tags:
    - mock
  operationId: post_api_v2_mock_openai_chat_completions
  description: 实现：`MockOpenAIController@chatCompletions`
  requestBody:
    required: false
    content:
      application/json:
        schema:
          type: object
          properties:
            delay:
              type: string
              default: h
            error:
              type: string
              default: h
            model:
              type: string
              default: gpt-3.5-turbo
            messages:
              type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
