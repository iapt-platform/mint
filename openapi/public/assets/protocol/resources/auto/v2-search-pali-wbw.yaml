# 由 openapi/scripts/generate.php 生成，请勿手改；补充写到 overrides/v2-search-pali-wbw.yaml
get:
  summary: '列出 search-pali-wbw'
  tags:
    - search-pali-wbw
  operationId: get_api_v2_search_pali_wbw
  description: 实现：`SearchPaliWbwController@index`
  parameters:
    -
      name: book
      in: query
      schema:
        type: string
    -
      name: tags
      in: query
      schema:
        type: string
    -
      name: key
      in: query
      schema:
        type: string
    -
      name: bold
      in: query
      schema:
        type: string
    -
      name: offset
      in: query
      schema:
        type: string
    -
      name: limit
      in: query
      schema:
        type: string
    -
      name: search
      in: query
      description: 搜索关键字
      schema:
        type: string
    -
      name: order
      in: query
      description: 排序字段
      schema:
        type: string
    -
      name: dir
      in: query
      description: 排序方向
      schema:
        type: string
        enum:
          - desc
          - asc
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
                properties:
                  rows:
                    type: array
                    items:
                      type: object
                      properties: { book: { type: string }, paragraph: { type: string }, type: { type: integer }, page: { type: string }, title: { type: string }, rank: { type: string }, path: { type: string }, paliTitle: { type: string }, link: { type: string }, highlight: { type: string }, ref: { type: string } }
                  count:
                    type: integer
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
post:
  summary: '新建 search-pali-wbw'
  tags:
    - search-pali-wbw
  operationId: post_api_v2_search_pali_wbw
  description: 实现：`SearchPaliWbwController@store`
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
