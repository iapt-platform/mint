# 由 openapi/scripts/generate.php 生成，请勿手改；补充写到 overrides/v2-group-member.yaml
get:
  summary: '列出 group-member'
  tags:
    - group-member
  operationId: get_api_v2_group_member
  description: 实现：`GroupMemberController@index`
  parameters:
    -
      name: view
      in: query
      schema:
        type: string
        enum:
          - group
          - user
    -
      name: id
      in: query
      schema:
        type: string
    -
      name: offset
      in: query
      schema:
        type: string
    -
      name: limit
      in: query
      schema:
        type: string
    -
      name: search
      in: query
      description: 搜索关键字
      schema:
        type: string
    -
      name: order
      in: query
      description: 排序字段
      schema:
        type: string
    -
      name: dir
      in: query
      description: 排序方向
      schema:
        type: string
        enum:
          - desc
          - asc
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
                properties:
                  rows:
                    type: array
                    items:
                      type: object
                      properties: { id: { type: integer }, user_id: { type: integer }, group_id: { type: integer }, group: { type: string }, power: { type: string }, role: { type: string }, level: { type: integer }, status: { type: integer }, user: { type: string }, created_at: { type: string }, updated_at: { type: string } }
                  count:
                    type: integer
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
post:
  summary: '新建 group-member'
  tags:
    - group-member
  operationId: post_api_v2_group_member
  description: 实现：`GroupMemberController@store`
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          properties:
            user_id:
              type: string
              description: 规则：required
            group_id:
              type: string
              description: 规则：required
          required:
            - user_id
            - group_id
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
                properties:
                  id:
                    type: integer
                  user_id:
                    type: integer
                  group_id:
                    type: integer
                  group:
                    type: string
                  power:
                    type: string
                  role:
                    type: string
                  level:
                    type: integer
                  status:
                    type: integer
                  user:
                    type: string
                  created_at:
                    type: string
                  updated_at:
                    type: string
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
