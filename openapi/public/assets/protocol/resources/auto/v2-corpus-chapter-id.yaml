# 由 openapi/scripts/generate.php 生成，请勿手改；补充写到 overrides/v2-corpus-chapter-id.yaml
get:
  summary: 'showChapter corpus-chapter/{id}'
  tags:
    - corpus-chapter
  operationId: get_api_v2_corpus_chapter_id_
  description: 实现：`CorpusController@showChapter`
  parameters:
    -
      name: id
      in: path
      required: true
      schema:
        type: string
    -
      name: debug
      in: query
      schema:
        type: string
    -
      name: channels
      in: query
      schema:
        type: string
    -
      name: mode
      in: query
      schema:
        type: string
        default: read
    -
      name: from
      in: query
      schema:
        type: string
    -
      name: to
      in: query
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
                properties:
                  book:
                    type: string
                  paragraph:
                    type: string
                  pali_title:
                    type: string
                  level:
                    type: integer
                  title:
                    type: string
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
