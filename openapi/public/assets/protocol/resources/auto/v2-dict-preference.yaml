# 由 openapi/scripts/generate.php 生成，请勿手改；补充写到 overrides/v2-dict-preference.yaml
get:
  summary: '列出 dict-preference'
  tags:
    - dict-preference
  operationId: get_api_v2_dict_preference
  description: 实现：`DictPreferenceController@index`
  parameters:
    -
      name: keyword
      in: query
      schema:
        type: string
    -
      name: order
      in: query
      schema:
        type: string
        default: word_indices.count
    -
      name: dir
      in: query
      schema:
        type: string
        default: desc
    -
      name: offset
      in: query
      schema:
        type: string
    -
      name: limit
      in: query
      schema:
        type: string
    -
      name: search
      in: query
      description: 搜索关键字
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
                properties:
                  rows:
                    type: array
                    items:
                      type: object
                      properties: { id: { type: integer }, word: { type: string }, count: { type: integer }, parent: { type: string }, note: { type: string }, factors: { type: string }, confidence: { type: string }, updated_at: { type: string }, creator_id: { type: integer }, editor: { type: string } }
                  count:
                    type: integer
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
post:
  summary: '新建 dict-preference'
  tags:
    - dict-preference
  operationId: post_api_v2_dict_preference
  description: 实现：`DictPreferenceController@store`
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
