# 由 openapi/scripts/generate.php 生成，请勿手改；补充写到 overrides/v2-grammar-guide-grammar-guide.yaml
get:
  summary: '获取单个 grammar-guide/{grammar_guide}'
  tags:
    - grammar-guide
  operationId: get_api_v2_grammar_guide_grammar_guide_
  description: 实现：`GrammarGuideController@show`
  parameters:
    -
      name: grammar_guide
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
put:
  summary: '更新 grammar-guide/{grammar_guide}'
  tags:
    - grammar-guide
  operationId: put_api_v2_grammar_guide_grammar_guide_
  description: 实现：`GrammarGuideController@update`
  parameters:
    -
      name: grammar_guide
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
patch:
  summary: '更新 grammar-guide/{grammar_guide}'
  tags:
    - grammar-guide
  operationId: patch_api_v2_grammar_guide_grammar_guide_
  description: 实现：`GrammarGuideController@update`
  parameters:
    -
      name: grammar_guide
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
delete:
  summary: '删除 grammar-guide/{grammar_guide}'
  tags:
    - grammar-guide
  operationId: delete_api_v2_grammar_guide_grammar_guide_
  description: 实现：`GrammarGuideController@destroy`
  parameters:
    -
      name: grammar_guide
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
