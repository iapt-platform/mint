# 由 openapi/scripts/generate.php 生成，请勿手改；补充写到 overrides/v2-nav-cs-para-nav-cs-para.yaml
get:
  summary: '获取单个 nav-cs-para/{nav_cs_para}'
  tags:
    - nav-cs-para
  operationId: get_api_v2_nav_cs_para_nav_cs_para_
  description: 实现：`NavCSParaController@show`
  parameters:
    -
      name: nav_cs_para
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
                properties:
                  content:
                    type: string
                  book:
                    type: string
                  start:
                    type: string
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
put:
  summary: '更新 nav-cs-para/{nav_cs_para}'
  tags:
    - nav-cs-para
  operationId: put_api_v2_nav_cs_para_nav_cs_para_
  description: 实现：`NavCSParaController@update`
  parameters:
    -
      name: nav_cs_para
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
patch:
  summary: '更新 nav-cs-para/{nav_cs_para}'
  tags:
    - nav-cs-para
  operationId: patch_api_v2_nav_cs_para_nav_cs_para_
  description: 实现：`NavCSParaController@update`
  parameters:
    -
      name: nav_cs_para
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
delete:
  summary: '删除 nav-cs-para/{nav_cs_para}'
  tags:
    - nav-cs-para
  operationId: delete_api_v2_nav_cs_para_nav_cs_para_
  description: 实现：`NavCSParaController@destroy`
  parameters:
    -
      name: nav_cs_para
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
