# 由 openapi/scripts/generate.php 生成，请勿手改；补充写到 overrides/v2-group-my-number.yaml
get:
  summary: 获取我的，和协作channel数量
  tags:
    - group-my-number
  operationId: get_api_v2_group_my_number
  description: 实现：`GroupController@showMyNumber`
  parameters:
    -
      name: studio
      in: query
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
