# 由 openapi/scripts/generate.php 生成，请勿手改；补充写到 overrides/v3-progress.yaml
get:
  summary: '列出 progress'
  tags:
    - progress
  operationId: get_api_v3_progress
  description: 实现：`ProgressController@index`
  parameters:
    -
      name: view
      in: query
      schema:
        type: string
        enum:
          - channel
    -
      name: channels
      in: query
      schema:
        type: string
        default: ''
    -
      name: level
      in: query
      schema:
        type: string
    -
      name: lang
      in: query
      schema:
        type: string
    -
      name: book
      in: query
      schema:
        type: string
    -
      name: order
      in: query
      schema:
        type: string
        default: completed_at
    -
      name: dir
      in: query
      schema:
        type: string
        default: desc
    -
      name: offset
      in: query
      schema:
        type: string
    -
      name: limit
      in: query
      schema:
        type: string
    -
      name: search
      in: query
      description: 搜索关键字
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
                properties:
                  rows:
                    type: array
                    items:
                      type: object
                  count:
                    type: integer
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
post:
  summary: '新建 progress'
  tags:
    - progress
  operationId: post_api_v3_progress
  description: 实现：`ProgressController@store`
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
