# 由 openapi/scripts/generate.php 生成，请勿手改；补充写到 overrides/v2-search-book-list.yaml
get:
  summary: 'book_list search-book-list'
  tags:
    - search-book-list
  operationId: get_api_v2_search_book_list
  description: 实现：`SearchController@book_list`
  parameters:
    -
      name: tags
      in: query
      schema:
        type: string
    -
      name: key
      in: query
      schema:
        type: string
    -
      name: view
      in: query
      schema:
        type: string
        enum:
          - pali
          - page
          - title
        default: pali
    -
      name: match
      in: query
      schema:
        type: string
        default: case
    -
      name: type
      in: query
      schema:
        type: string
        default: P
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
                properties:
                  pcdBookId:
                    type: string
                  count:
                    type: integer
                  book:
                    type: string
                  paragraph:
                    type: string
                  paliTitle:
                    type: string
                  tags:
                    type: string
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
