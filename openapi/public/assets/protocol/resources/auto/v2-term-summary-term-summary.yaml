# 由 openapi/scripts/generate.php 生成，请勿手改；补充写到 overrides/v2-term-summary-term-summary.yaml
get:
  summary: '获取单个 term-summary/{term_summary}'
  tags:
    - term-summary
  operationId: get_api_v2_term_summary_term_summary_
  description: 实现：`TermSummaryController@show`
  parameters:
    -
      name: term_summary
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
put:
  summary: '更新 term-summary/{term_summary}'
  tags:
    - term-summary
  operationId: put_api_v2_term_summary_term_summary_
  description: 实现：`TermSummaryController@update`
  parameters:
    -
      name: term_summary
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
patch:
  summary: '更新 term-summary/{term_summary}'
  tags:
    - term-summary
  operationId: patch_api_v2_term_summary_term_summary_
  description: 实现：`TermSummaryController@update`
  parameters:
    -
      name: term_summary
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
delete:
  summary: '删除 term-summary/{term_summary}'
  tags:
    - term-summary
  operationId: delete_api_v2_term_summary_term_summary_
  description: 实现：`TermSummaryController@destroy`
  parameters:
    -
      name: term_summary
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
