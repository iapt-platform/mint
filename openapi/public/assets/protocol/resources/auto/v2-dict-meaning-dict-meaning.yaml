# 由 openapi/scripts/generate.php 生成，请勿手改；补充写到 overrides/v2-dict-meaning-dict-meaning.yaml
get:
  summary: '获取单个 dict-meaning/{dict_meaning}'
  tags:
    - dict-meaning
  operationId: get_api_v2_dict_meaning_dict_meaning_
  description: 实现：`DictMeaningController@show`
  parameters:
    -
      name: dict_meaning
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
put:
  summary: '更新 dict-meaning/{dict_meaning}'
  tags:
    - dict-meaning
  operationId: put_api_v2_dict_meaning_dict_meaning_
  description: 实现：`DictMeaningController@update`
  parameters:
    -
      name: dict_meaning
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
patch:
  summary: '更新 dict-meaning/{dict_meaning}'
  tags:
    - dict-meaning
  operationId: patch_api_v2_dict_meaning_dict_meaning_
  description: 实现：`DictMeaningController@update`
  parameters:
    -
      name: dict_meaning
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
delete:
  summary: '删除 dict-meaning/{dict_meaning}'
  tags:
    - dict-meaning
  operationId: delete_api_v2_dict_meaning_dict_meaning_
  description: 实现：`DictMeaningController@destroy`
  parameters:
    -
      name: dict_meaning
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
