# 由 openapi/scripts/generate.php 生成，请勿手改；补充写到 overrides/v2-share.yaml
get:
  summary: '列出 share'
  tags:
    - share
  operationId: get_api_v2_share
  description: 实现：`ShareController@index`
  parameters:
    -
      name: view
      in: query
      schema:
        type: string
        enum:
          - res
          - group
    -
      name: id
      in: query
      schema:
        type: string
    -
      name: offset
      in: query
      schema:
        type: string
    -
      name: limit
      in: query
      schema:
        type: string
    -
      name: search
      in: query
      description: 搜索关键字
      schema:
        type: string
    -
      name: order
      in: query
      description: 排序字段
      schema:
        type: string
    -
      name: dir
      in: query
      description: 排序方向
      schema:
        type: string
        enum:
          - desc
          - asc
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
                properties:
                  rows:
                    type: array
                    items:
                      type: object
                      properties: { id: { type: integer }, res_id: { type: integer }, res_type: { type: string }, collaborator_type: { type: string }, power: { type: string }, res_name: { type: string }, owner: { type: string }, created_at: { type: string }, updated_at: { type: string }, user: { type: string }, group: { type: string } }
                  count:
                    type: integer
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
post:
  summary: '新建 share'
  tags:
    - share
  operationId: post_api_v2_share
  description: 实现：`ShareController@store`
  requestBody:
    required: false
    content:
      application/json:
        schema:
          type: object
          properties:
            user_id:
              type: string
            res_id:
              type: string
            res_type:
              type: string
            user_type:
              type: string
            role:
              type: string
              enum:
                - manager
                - editor
                - reader
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
