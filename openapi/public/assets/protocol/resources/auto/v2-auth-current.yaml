# 由 openapi/scripts/generate.php 生成，请勿手改；补充写到 overrides/v2-auth-current.yaml
get:
  summary: 'getUserInfoByToken auth/current'
  tags:
    - auth
  operationId: get_api_v2_auth_current
  description: 实现：`AuthController@getUserInfoByToken`
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
                properties:
                  id:
                    type: string
                  nickName:
                    type: string
                  realName:
                    type: string
                  avatar:
                    type: string
                  roles:
                    type: array
                    items:
                      type: string
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
