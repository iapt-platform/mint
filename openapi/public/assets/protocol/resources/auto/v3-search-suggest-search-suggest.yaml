# 由 openapi/scripts/generate.php 生成，请勿手改；补充写到 overrides/v3-search-suggest-search-suggest.yaml
get:
  summary: '获取单个 search-suggest/{search_suggest}'
  tags:
    - search-suggest
  operationId: get_api_v3_search_suggest_search_suggest_
  description: 实现：`SearchSuggestController@show`
  parameters:
    -
      name: search_suggest
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
put:
  summary: '更新 search-suggest/{search_suggest}'
  tags:
    - search-suggest
  operationId: put_api_v3_search_suggest_search_suggest_
  description: 实现：`SearchSuggestController@update`
  parameters:
    -
      name: search_suggest
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
patch:
  summary: '更新 search-suggest/{search_suggest}'
  tags:
    - search-suggest
  operationId: patch_api_v3_search_suggest_search_suggest_
  description: 实现：`SearchSuggestController@update`
  parameters:
    -
      name: search_suggest
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
delete:
  summary: '删除 search-suggest/{search_suggest}'
  tags:
    - search-suggest
  operationId: delete_api_v3_search_suggest_search_suggest_
  description: 实现：`SearchSuggestController@destroy`
  parameters:
    -
      name: search_suggest
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
