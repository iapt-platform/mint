# 由 openapi/scripts/generate.php 生成，请勿手改；补充写到 overrides/v2-transfer.yaml
get:
  summary: '列出 transfer'
  tags:
    - transfer
  operationId: get_api_v2_transfer
  description: 实现：`TransferController@index`
  parameters:
    -
      name: view
      in: query
      schema:
        type: string
        enum:
          - studio
    -
      name: name
      in: query
      schema:
        type: string
    -
      name: view2
      in: query
      schema:
        type: string
        enum:
          - in
          - out
    -
      name: order
      in: query
      schema:
        type: string
        default: updated_at
    -
      name: dir
      in: query
      schema:
        type: string
        default: desc
    -
      name: offset
      in: query
      schema:
        type: string
    -
      name: limit
      in: query
      schema:
        type: string
    -
      name: search
      in: query
      description: 搜索关键字
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
                properties:
                  rows:
                    type: array
                    items:
                      type: object
                      properties: { id: { type: integer }, origin_owner: { type: string }, res_type: { type: string }, res_id: { type: integer }, transferor: { type: string }, new_owner: { type: string }, status: { type: integer }, updated_at: { type: string }, created_at: { type: string }, editor: { type: string }, channel: { type: string } }
                  count:
                    type: integer
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
post:
  summary: '新建 transfer'
  tags:
    - transfer
  operationId: post_api_v2_transfer
  description: 实现：`TransferController@store`
  requestBody:
    required: false
    content:
      application/json:
        schema:
          type: object
          properties:
            res_id:
              type: string
            res_type:
              type: string
              enum:
                - channel
                - article
            new_owner:
              type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
