# 由 openapi/scripts/generate.php 生成，请勿手改；补充写到 overrides/v2-invite.yaml
get:
  summary: '列出 invite'
  tags:
    - invite
  operationId: get_api_v2_invite
  description: 实现：`InviteController@index`
  parameters:
    -
      name: view
      in: query
      schema:
        type: string
        enum:
          - studio
          - all
    -
      name: studio
      in: query
      schema:
        type: string
    -
      name: search
      in: query
      schema:
        type: string
    -
      name: order
      in: query
      schema:
        type: string
        default: updated_at
    -
      name: dir
      in: query
      schema:
        type: string
        default: desc
    -
      name: offset
      in: query
      schema:
        type: string
    -
      name: limit
      in: query
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
                properties:
                  rows:
                    type: array
                    items:
                      type: object
                  count:
                    type: integer
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
post:
  summary: '新建 invite'
  tags:
    - invite
  operationId: post_api_v2_invite
  description: 实现：`InviteController@store`
  requestBody:
    required: false
    content:
      application/json:
        schema:
          type: object
          properties:
            studio:
              type: string
            email:
              type: string
            subject:
              type: string
              default: 'sign up wikipali'
            lang:
              type: string
            dashboard:
              type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
