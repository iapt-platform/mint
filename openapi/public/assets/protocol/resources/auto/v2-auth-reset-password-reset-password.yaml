# 由 openapi/scripts/generate.php 生成，请勿手改；补充写到 overrides/v2-auth-reset-password-reset-password.yaml
get:
  summary: 根据token获取用户名.
  tags:
    - auth
  operationId: get_api_v2_auth_reset_password_reset_password_
  description: 实现：`ResetPasswordController@show`
  parameters:
    -
      name: reset_password
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
put:
  summary: '更新 auth/reset-password/{reset_password}'
  tags:
    - auth
  operationId: put_api_v2_auth_reset_password_reset_password_
  description: 实现：`ResetPasswordController@update`
  parameters:
    -
      name: reset_password
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
patch:
  summary: '更新 auth/reset-password/{reset_password}'
  tags:
    - auth
  operationId: patch_api_v2_auth_reset_password_reset_password_
  description: 实现：`ResetPasswordController@update`
  parameters:
    -
      name: reset_password
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
delete:
  summary: '删除 auth/reset-password/{reset_password}'
  tags:
    - auth
  operationId: delete_api_v2_auth_reset_password_reset_password_
  description: 实现：`ResetPasswordController@destroy`
  parameters:
    -
      name: reset_password
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
