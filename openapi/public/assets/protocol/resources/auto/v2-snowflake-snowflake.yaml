# 由 openapi/scripts/generate.php 生成，请勿手改；补充写到 overrides/v2-snowflake-snowflake.yaml
get:
  summary: '获取单个 snowflake/{snowflake}'
  tags:
    - snowflake
  operationId: get_api_v2_snowflake_snowflake_
  description: 实现：`SnowFlakeIdController@show`
  parameters:
    -
      name: snowflake
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
put:
  summary: '更新 snowflake/{snowflake}'
  tags:
    - snowflake
  operationId: put_api_v2_snowflake_snowflake_
  description: 实现：`SnowFlakeIdController@update`
  parameters:
    -
      name: snowflake
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
patch:
  summary: '更新 snowflake/{snowflake}'
  tags:
    - snowflake
  operationId: patch_api_v2_snowflake_snowflake_
  description: 实现：`SnowFlakeIdController@update`
  parameters:
    -
      name: snowflake
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
delete:
  summary: '删除 snowflake/{snowflake}'
  tags:
    - snowflake
  operationId: delete_api_v2_snowflake_snowflake_
  description: 实现：`SnowFlakeIdController@destroy`
  parameters:
    -
      name: snowflake
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
