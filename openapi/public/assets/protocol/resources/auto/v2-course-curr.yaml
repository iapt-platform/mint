# 由 openapi/scripts/generate.php 生成，请勿手改；补充写到 overrides/v2-course-curr.yaml
get:
  summary: '获取当前用户在指定课程中的角色与 channel'
  tags:
    - course-curr
  operationId: get_api_v2_course_curr
  description: |-
    必须登录。只读取 is_current=true 的记录，返回 role 与 channel_id 两个字段。
    当前用户不是该课程成员时返回 not member。

    实现：`CourseMemberController@curr`
  parameters:
    -
      name: course_id
      in: query
      schema:
        type: string
      description: 'course uid'
      required: true
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
