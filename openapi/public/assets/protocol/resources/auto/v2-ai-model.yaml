# 由 openapi/scripts/generate.php 生成，请勿手改；补充写到 overrides/v2-ai-model.yaml
get:
  summary: '列出 ai-model'
  tags:
    - ai-model
  operationId: get_api_v2_ai_model
  description: 实现：`AiModelController@index`
  parameters:
    -
      name: view
      in: query
      schema:
        type: string
        enum:
          - all
          - studio
          - usable
          - chat
    -
      name: name
      in: query
      schema:
        type: string
    -
      name: user_id
      in: query
      schema:
        type: string
    -
      name: keyword
      in: query
      schema:
        type: string
    -
      name: order
      in: query
      schema:
        type: string
        default: created_at
    -
      name: dir
      in: query
      schema:
        type: string
        default: asc
    -
      name: offset
      in: query
      schema:
        type: string
    -
      name: limit
      in: query
      schema:
        type: string
    -
      name: search
      in: query
      description: 搜索关键字
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
                properties:
                  rows:
                    type: array
                    items:
                      type: object
                      properties: { uid: { type: string }, name: { type: string }, description: { type: string }, url: { type: string }, model: { type: string }, privacy: { type: string }, owner_id: { type: integer }, editor_id: { type: integer }, created_at: { type: string }, updated_at: { type: string }, user: { type: string }, key: { type: string }, system_prompt: { type: string } }
                  count:
                    type: integer
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
post:
  summary: '新建 ai-model'
  tags:
    - ai-model
  operationId: post_api_v2_ai_model
  description: 实现：`AiModelController@store`
  requestBody:
    required: false
    content:
      application/json:
        schema:
          type: object
          properties:
            studio_name:
              type: string
            name:
              type: string
            privacy:
              type: string
              default: private
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
                properties:
                  uid:
                    type: string
                  name:
                    type: string
                  description:
                    type: string
                  url:
                    type: string
                  model:
                    type: string
                  privacy:
                    type: string
                  owner_id:
                    type: integer
                  editor_id:
                    type: integer
                  created_at:
                    type: string
                  updated_at:
                    type: string
                  user:
                    type: string
                  key:
                    type: string
                  system_prompt:
                    type: string
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
