# 由 openapi/scripts/generate.php 生成，请勿手改；补充写到 overrides/v2-sent-sim.yaml
get:
  summary: '列出 sent-sim'
  tags:
    - sent-sim
  operationId: get_api_v2_sent_sim
  description: 实现：`SentSimController@index`
  parameters:
    -
      name: view
      in: query
      schema:
        type: string
        enum:
          - sentence
    -
      name: book
      in: query
      schema:
        type: string
    -
      name: paragraph
      in: query
      schema:
        type: string
    -
      name: start
      in: query
      schema:
        type: string
    -
      name: end
      in: query
      schema:
        type: string
    -
      name: sim
      in: query
      schema:
        type: string
    -
      name: offset
      in: query
      schema:
        type: string
    -
      name: limit
      in: query
      schema:
        type: string
    -
      name: search
      in: query
      description: 搜索关键字
      schema:
        type: string
    -
      name: order
      in: query
      description: 排序字段
      schema:
        type: string
    -
      name: dir
      in: query
      description: 排序方向
      schema:
        type: string
        enum:
          - desc
          - asc
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
                properties:
                  rows:
                    type: array
                    items:
                      type: object
                      properties: { sent: { type: string }, sim: { type: string } }
                  count:
                    type: integer
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
post:
  summary: '新建 sent-sim'
  tags:
    - sent-sim
  operationId: post_api_v2_sent_sim
  description: 实现：`SentSimController@store`
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
