# 由 openapi/scripts/generate.php 生成，请勿手改；补充写到 overrides/v2-attachments-attachment.yaml
get:
  summary: '获取单个 attachments/{attachment}'
  tags:
    - attachments
  operationId: get_api_v2_attachments_attachment_
  description: 实现：`UploadController@show`
  parameters:
    -
      name: attachment
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
put:
  summary: '更新 attachments/{attachment}'
  tags:
    - attachments
  operationId: put_api_v2_attachments_attachment_
  description: 实现：`UploadController@update`
  parameters:
    -
      name: attachment
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
patch:
  summary: '更新 attachments/{attachment}'
  tags:
    - attachments
  operationId: patch_api_v2_attachments_attachment_
  description: 实现：`UploadController@update`
  parameters:
    -
      name: attachment
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
delete:
  summary: '删除 attachments/{attachment}'
  tags:
    - attachments
  operationId: delete_api_v2_attachments_attachment_
  description: 实现：`UploadController@destroy`
  parameters:
    -
      name: attachment
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
