# 由 openapi/scripts/generate.php 生成，请勿手改；补充写到 overrides/v2-nissaya-ending-vocabulary.yaml
get:
  summary: 'vocabulary nissaya-ending-vocabulary'
  tags:
    - nissaya-ending-vocabulary
  operationId: get_api_v2_nissaya_ending_vocabulary
  description: 实现：`NissayaEndingController@vocabulary`
  parameters:
    -
      name: lang
      in: query
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
