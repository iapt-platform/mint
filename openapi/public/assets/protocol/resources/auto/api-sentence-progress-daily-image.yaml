# 由 openapi/scripts/generate.php 生成，请勿手改；补充写到 overrides/api-sentence-progress-daily-image.yaml
get:
  summary: 'showprogressdaily api/api/sentence/progress/daily/image'
  tags:
    - api
  operationId: get_api_api_sentence_progress_daily_image
  description: 实现：`SentenceInfoController@showprogressdaily`
  parameters:
    -
      name: view
      in: query
      schema:
        type: string
    -
      name: type
      in: query
      schema:
        type: string
    -
      name: channel
      in: query
      schema:
        type: string
    -
      name: from
      in: query
      schema:
        type: string
    -
      name: to
      in: query
      schema:
        type: string
    -
      name: book
      in: query
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
