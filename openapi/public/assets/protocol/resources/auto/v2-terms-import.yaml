# 由 openapi/scripts/generate.php 生成，请勿手改；补充写到 overrides/v2-terms-import.yaml
get:
  summary: 'import terms-import'
  tags:
    - terms-import
  operationId: get_api_v2_terms_import
  description: 实现：`TermExportController@import`
  parameters:
    -
      name: view
      in: query
      schema:
        type: string
        enum:
          - channel
          - studio
    -
      name: id
      in: query
      schema:
        type: string
    -
      name: name
      in: query
      schema:
        type: string
    -
      name: filename
      in: query
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
