# 由 openapi/scripts/generate.php 生成，请勿手改；补充写到 overrides/v2-nissaya-card-nissaya-card.yaml
get:
  summary: '获取单个 nissaya-card/{nissaya_card}'
  tags:
    - nissaya-card
  operationId: get_api_v2_nissaya_card_nissaya_card_
  description: 实现：`NissayaCardController@show`
  parameters:
    -
      name: nissaya_card
      in: path
      required: true
      schema:
        type: string
    -
      name: lang
      in: query
      schema:
        type: string
    -
      name: content_type
      in: query
      schema:
        type: string
        default: markdown
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
put:
  summary: '更新 nissaya-card/{nissaya_card}'
  tags:
    - nissaya-card
  operationId: put_api_v2_nissaya_card_nissaya_card_
  description: 实现：`NissayaCardController@update`
  parameters:
    -
      name: nissaya_card
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
patch:
  summary: '更新 nissaya-card/{nissaya_card}'
  tags:
    - nissaya-card
  operationId: patch_api_v2_nissaya_card_nissaya_card_
  description: 实现：`NissayaCardController@update`
  parameters:
    -
      name: nissaya_card
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
delete:
  summary: '删除 nissaya-card/{nissaya_card}'
  tags:
    - nissaya-card
  operationId: delete_api_v2_nissaya_card_nissaya_card_
  description: 实现：`NissayaCardController@destroy`
  parameters:
    -
      name: nissaya_card
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: '成功。业务失败时 HTTP 仍可能为 200，以 ok 字段为准。'
      content:
        application/json:
          schema:
            type: object
            properties:
              ok:
                type: boolean
              message:
                type: string
              data:
                type: object
    401:
      $ref: '../../main.yaml#/components/responses/Unauthorized'
    422:
      $ref: '../../main.yaml#/components/responses/ValidationError'
